'use strict';

var _gulp = require('gulp');

var _gulp2 = _interopRequireDefault(_gulp);

var _coldboxElixir = require('coldbox-elixir');

var _coldboxElixir2 = _interopRequireDefault(_coldboxElixir);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
 |----------------------------------------------------------------
 | Shell Commands
 |----------------------------------------------------------------
 |
 | Need to execute a shell script, as part of your compile
 | process? No problem. Elixir can help with that. Just
 | call `mix.exec('command')`, and, bam, you're set!
 |
 */

_coldboxElixir2.default.extend('exec', function (command, watcher) {
    var task = new _coldboxElixir2.default.Task('exec', function () {
        _coldboxElixir2.default.Log.heading('Triggering Command...').message(command);

        return _gulp2.default.src('').pipe(_coldboxElixir2.default.Plugins.shell(command));
    });

    if (watcher) {
        task.watch(watcher);
    }
});