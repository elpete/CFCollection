'use strict';

var _gulp = require('gulp');

var _gulp2 = _interopRequireDefault(_gulp);

var _coldboxElixir = require('coldbox-elixir');

var _coldboxElixir2 = _interopRequireDefault(_coldboxElixir);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
 |----------------------------------------------------------------
 | TDD Watcher
 |----------------------------------------------------------------
 |
 | This task will keep an eye on any tasks that are part of the
 | bdd category. By default this includes TestBox XUnit
 | tests. Run `gulp bdd` and your tests will auto-trigger.
 |
 */

_gulp2.default.task('tdd', function () {
  new _coldboxElixir2.default.Log.message('Watching for specs...');

  _coldboxElixir2.default.tasks.filter(function (task) {
    return task.category == 'tdd';
  }).forEach(function (task) {
    return _gulp2.default.watch(task.watchers, [task.name]);
  });
});