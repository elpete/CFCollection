'use strict';

var _gulp = require('gulp');

var _gulp2 = _interopRequireDefault(_gulp);

var _gulpUtil = require('gulp-util');

var _gulpUtil2 = _interopRequireDefault(_gulpUtil);

var _coldboxElixir = require('coldbox-elixir');

var _coldboxElixir2 = _interopRequireDefault(_coldboxElixir);

var _runSequence = require('run-sequence');

var _runSequence2 = _interopRequireDefault(_runSequence);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
 *----------------------------------------------------------------
 * Default Task
 *----------------------------------------------------------------
 *
 * This task will run when the developer executes "gulp" on the
 * command line. We'll use this configuration object to know
 * which tasks should be fired when this task is executed.
 * 
 * If not tasks are defined, this task will report it and end execution
 */

_gulp2.default.task('default', function () {
  var tasks = _coldboxElixir2.default.tasks.names();

  // Verify length
  if (tasks.length) {
    _runSequence2.default.apply(this, _coldboxElixir2.default.tasks.names());
  } else {
    _gulpUtil2.default.log("No tasks defined in your 'Gulpfile.js'.  So I am exiting the beautiful world of ColdBox Elixir!");
  }
});